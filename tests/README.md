# Test Suite Layout

- `tests/unit/` — Vitest specs for hooks and utilities once they are implemented.
- `tests/e2e/` — Playwright smoke flows for clipboard, docs links, and GitHub badge updates.

Scripts will live under `package.json` when the suites are populated.
